#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include <string.h>

int main(void)
{
ssize_t bytes_read;
char input_buffer[1024];
pid_t parent_pid;

parent_pid = getpid();

while (1)
{
/* Prints prompt */
printf("$ ");

/* Reads user input */
bytes_read = getline(input_buffer, sizeof(input_buffer), stdin);
if (bytes_read == -1)
{
perror("getline error");
exit(EXIT_FAILURE);
}

/* Removes trailing newline */
input_buffer[strcspn(input_buffer, "\n")] = '\0';

/* Processes user input (later implement commands and handling) */
/* Checks for built-in commands (cd, exit, etc.) */
/* Calculates max PID if necessary (avoid external scripts) */

/* Clears buffer for next input */
memset(input_buffer, 0, sizeof(buffer));
}

return (0);
}
